name: test
channels:
  - conda-forge
  - openeye
  - psi4
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Standard dependencies.
  - jinja2
  - numpy
  - pydantic
  - openeye-toolkits

    # Optional dependencies.
  - psi4
  - distributed
  - dask-jobqueue

    # Python < 3.8
  - typing-extensions