name: test
channels:
  - conda-forge
  - omnia
  - openeye
  - psi4/label/dev
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    ### Core dependencies.

  - numpy
  - pydantic
  - openeye-toolkits

    # ESP calculations
  - jinja2
  - psi4

    # ESP storage
  - sqlalchemy
  - sqlite

    # Python < 3.8
  - typing-extensions

    ### Optional dependencies.

    # SMIRNOFF
  - openforcefield >= 0.7.0

    # Linting
  - black
  - isort
  - flake8
