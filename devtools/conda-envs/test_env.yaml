name: test
channels:
  - conda-forge
  - openeye
  - psi4/label/dev
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    ### Core dependencies.

  - numpy
  - pydantic
  - openeye-toolkits

    # ESP calculations
  - jinja2
  - psi4

    # ESP storage
  - sqlalchemy
  - sqlite

    # Python < 3.8
  - typing-extensions

    ### Optional dependencies.

    # Linting
  - black
  - isort
  - flake8

    # Python < 3.8
  - typing-extensions
